#if (${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end

import com.dvsnier.common.presenter.BaseCompatPresenter;

#parse("MVP File Header.java")
public class ${NAME} extends BaseCompatPresenter<${Activity_Name}> {

    public void request() {
        // TODO: ${DATE}
    }
}
