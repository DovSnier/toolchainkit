
# 清理，构建
./gradlew clean build

# 构建到 Maven
./gradlew uploadArchives

# 安装到本地
./gradlew install

# 发布到 Maven
#./gradlew clean build publishToMavenLocal bintrayUpload -PdryRun=false
./gradlew :base:clean :base:build :base:publishToMavenLocal
#举例说明如下：
    ./gradlew --stacktrace --info :base:clean :base:build
    ./gradlew --stacktrace --info :base:clean :base:build :base:publishToMavenLocal
    ./gradlew --stacktrace --info :base:publishToMavenLocal
    ./gradlew --stacktrace --info clean build publishToMavenLocal

# 发布 bintray
./gradlew clean build bintrayUpload -PbintrayUser=BINTRAY_USER -PbintrayKey=BINTRAY_API_KEY -PdryRun=false
#举例说明如下：
    ./gradlew :base:bintrayUpload -PbintrayUser=BINTRAY_USER -PbintrayKey=BINTRAY_API_KEY -PdryRun=false
    ./gradlew :base:clean :base:build :base:bintrayUpload -PbintrayUser=BINTRAY_USER -PbintrayKey=BINTRAY_API_KEY -PdryRun=false
